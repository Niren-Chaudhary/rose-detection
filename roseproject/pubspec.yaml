name: roseproject
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  animate_do: ^3.1.2
  http: ^1.2.2
  dio: ^4.0.0
  path: ^1.9.0
  camera: ^0.11.0
  image_picker: ^1.1.2
  shared_preferences: ^2.0.17  # Keep this line

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
